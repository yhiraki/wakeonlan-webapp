apiVersion: v1
kind: Service
metadata:
  name: wakeonlan-webapp
  namespace: wakeonlan-webapp
  annotations:
    tailscale.com/hostname: wol
spec:
  selector:
    app: wakeonlan-webapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
  loadBalancerClass: tailscale
